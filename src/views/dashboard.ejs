<%-include ('partials/header')%>
<main>
    <h1><%=title%></h1>
    <strong>Username: <%=email%></strong>
    <% if (user && user.role_name === 'admin') {%>
        <p class="page-action">
            <a href="users-list">See list of registered users</a>
        </p>
    <%}%>    
</main>
<%-include ('partials/footer')%>